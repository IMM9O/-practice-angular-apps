import { CountSizePipe } from './count-size.pipe';

describe('CountSizePipe', () => {
  it('create an instance', () => {
    const pipe = new CountSizePipe();
    expect(pipe).toBeTruthy();
  });
});
